<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_red_light"
    tools:context="extopoint.suganya.youtubeplayer.MainActivity">

    <ImageView
        android:id="@+id/welcomeimage"
        android:layout_width="100dp"
        android:layout_height="75dp"
        android:layout_above="@+id/butnplaylist"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="78dp"
        android:background="@android:color/holo_red_dark"/>

    <Button
        android:id="@+id/butnplaylist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Playlist"
        android:textStyle="bold|italic"/>

    <Button
        android:id="@+id/butntrending"
        android:layout_centerInParent="true"
        android:layout_below="@+id/butnplaylist"
        android:layout_marginTop="25dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Trending Videos"
        android:textStyle="bold|italic"/>

</RelativeLayout>
